services:
  postgres:
    image: postgres:17
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: prisma
      POSTGRES_DB: graphql_prisma
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
